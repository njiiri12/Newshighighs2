{% extends 'base.html'%}

{% block styles %}
{{ super() }}
<link rel="stylesheet" href="{{ url_for('static',filename='css/article.css') }}">
<link rel="preconnect" href="https://fonts.gstatic.com">
<link href="https://fonts.googleapis.com/css2?family=Merriweather+Sans:wght@300;400&display=swap" rel="stylesheet">
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/css/bootstrap.min.css"
    integrity="sha384-B0vP5xmATw1+K9KRQjQERJvTumQW0nPEzvF6L/Z6nronJ3oUOFUFpCjEUQouq2+l" crossorigin="anonymous">
<link rel="preconnect" href="https://fonts.gstatic.com">
<link href="https://fonts.googleapis.com/css2?family=Work+Sans:wght@500&display=swap" rel="stylesheet">
<script>
    function openNav() {
        document.getElementById("mySidebar").style.width = "250px";
    }

    function closeNav() {
        document.getElementById("mySidebar").style.width = "0";
        document.getElementById("main").style.marginLeft = "0";
    }
</script>
{% endblock %}

{% block content %}
<div id="mySidebar" class="sidebar">
    <a href="javascript:void(0)" class="closebtn" onclick="closeNav()">×</a>
    {% for source_news in news %}
    <a href="/articles/{{source_news.id}}">
        {{ source_news.name }}</a>
    {% endfor %}
</div>
<h1 style="text-align: center; font-family: 'Merriweather Sans', sans-serif;">{{ id }} NEWS ARTICLES</h1>
<div class="container-fluid">
    <div class="card-columns">
        {% for source_article in article %}
        <a href="{{ source_article.url }}" style="color: inherit; text-decoration: none;">
            <div class="card" style="width: 40rem; font-size: 15px;">
                <img src="{{ source_article.urlToImage }}" class="card-img-top" alt="...">
                <div class="card-body" style="margin-top: 10px;">
                    <h5 class="card-title" style="font-size: 20px; font-weight: bold;">{{ source_article.title }}</h5>
                    <p class="card-text">{{ source_article.description }}</p>
                    <p class="card-text"><small class="text-muted">Posted:
                            {{ source_article.publishedAt }}</small></p>
                    <hr>
                    <a href="{{ source_article.url }}" class="btn btn-secondary btn-lg">Read The Full Article</a>
                </div>
            </div>
        </a>
        {% endfor %}
    </div>
</div>
{% endblock %}